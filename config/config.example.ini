; Copy to config.ini for local development. config.ini is ignored by Git.

[network]
host=127.0.0.1
port=9527

[database]
; Empty means the Server will use QStandardPaths::AppDataLocation.
path=
seed_demo_data=false

[map]
; Prefer the CHARGING_TENCENT_MAP_KEY environment variable when implemented.
tencent_api_key=

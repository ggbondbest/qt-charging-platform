# Profile/Charging Pages（成员 3）

个人中心、充值、订单、充电和结算页面放在本目录。

当前为 `STATIC` target（成员3 本地开发中，未提交）。已实现：`WalletPage`、
`RechargePage`；`preview/preview_main.cpp` + `charging-profile-preview` 仅为本地
mock 预览壳，不属于 `charging_client` 正式装配，接入导航机制后移除。
